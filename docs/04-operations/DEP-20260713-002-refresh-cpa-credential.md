# DEP-20260713-002 CPA 凭证刷新功能部署

## 基本信息

- 环境：服务器 8318 服务
- 部署日期：2026-07-13
- 对应变更：CHG-20260713-002
- 结果：成功

## 部署范围

- 成功账户“刷新凭证”按钮。
- Web 任务参数与补全脚本刷新模式。
- xAI refresh_token 请求与 CPA 安全回写模块。
- OIDC 兜底和旧凭证恢复逻辑。

## 部署步骤

1. 在服务器临时目录保留部署前文件。
2. 上传 Web、OAuth、凭证刷新和补全任务文件。
3. 在服务器虚拟环境执行 Python 语法检查。
4. 重启 8318 Web 服务。
5. 检查成功账户页面按钮。
6. 对一个已有 CPA 账户执行真实 refresh_token 刷新。
7. 验证刷新后的模型请求。
8. 验证成功后删除临时回滚目录。

## 健康检查

- 服务状态：`active`
- 已有 CPA 账户显示“刷新凭证”按钮。
- refresh_token 请求已执行成功。
- 未触发 OIDC 兜底。
- 刷新后 `grok-4.5` 模型请求返回 HTTP 200。

## 验证结果

- 线上按钮数量与已有 CPA 账户一致。
- 刷新任务退出码为 0。
- 日志包含 refresh_token 尝试和成功节点。
- 新凭证模型权限验证成功。
- 临时回滚副本已经删除。

## 回滚方案

- 恢复部署前的 Web、OAuth 和补全任务文件并重启服务。
- 新增模块 `cpa_xai/refresh.py` 可以随代码一起移除。
- 已刷新凭证继续兼容现有 CPA 使用方式。

## 安全检查

- [x] 文档未包含密码、私钥或令牌
- [x] 日志未输出完整凭证

