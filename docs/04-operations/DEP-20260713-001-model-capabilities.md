# DEP-20260713-001 模型能力管理部署

## 基本信息

- 环境：服务器 8318 服务
- 部署日期：2026-07-13
- 对应变更：CHG-20260713-001
- 结果：成功

## 部署范围

- Web 后端模型与额度接口。
- Web 前端模型 Tag 和操作按钮。
- CPA 模型探测与模型能力缓存模块。

## 部署步骤

1. 在服务器临时目录保留部署前文件用于即时回滚。
2. 上传变更文件和新增缓存模块。
3. 在服务器虚拟环境执行 Python 语法检查。
4. 重启 Web 服务。
5. 验证服务状态、模型接口和账户页面。
6. 验证成功后删除临时回滚目录。

## 健康检查

- 服务状态：`active`
- 页面：成功账户页可以显示模型 Tag 和操作按钮。
- 模型列表：成功返回 `grok-4.5`。
- 单模型测试：HTTP 200。
- 额度状态：1/1 个模型可用。

## 验证结果

- 代理总开关已开启。
- Mihomo 代理运行正常。
- 模型列表、测试和额度状态请求均通过代理完成。
- 部署后的临时回滚副本已经删除。

## 回滚方案

- 如果服务启动失败，恢复部署前文件并重启服务。
- 如果只出现模型缓存异常，可以删除 `.model_capabilities/` 后重新刷新模型。

## 安全检查

- [x] 文档未包含密码、私钥或令牌
- [x] 日志未输出完整凭证

