# 项目状态与交接

这里是后续开发读取项目现状的第一入口。

- `current-status.md` 保存当前功能、正在进行的工作、部署状态和建议下一步。
- `known-issues.md` 保存仍未解决的问题、风险、触发条件和临时规避方式。
- `progress-log.md` 按时间保存已经正式归档的开发进度。

状态页不是实时自动生成的。只有用户明确要求写入开发文档时才更新，因此阅读时应同时注意文档顶部的更新时间。

