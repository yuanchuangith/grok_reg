# CHG-20260713-001 成功账户模型 Tag、测试与额度状态

## 基本信息

- 状态：已完成
- 完成日期：2026-07-13
- 对应需求：REQ-20260713-001
- 部署记录：DEP-20260713-001

## 变更内容

- 成功账户增加可用模型列和状态 Tag。
- 增加刷新模型列表、单模型测试和刷新额度状态操作。
- 增加独立模型能力缓存，不修改 CPA 凭证 JSON。
- 解析常见 `x-ratelimit-*` 和 `retry-after` 响应头。
- 模型相关请求强制通过当前代理池。
- 对网络状态 0 和部分代理类 HTTP 错误增加最多三次重试。
- 新 CPA 生成并探测模型后自动写入模型列表缓存。

## 影响文件

- `web_dashboard.py:326`
- `webui/app.js:168`
- `webui/index.html:124`
- `webui/styles.css:86`
- `cpa_xai/probe.py:35`
- `cpa_xai/mint.py:180`
- `cpa_xai/model_capabilities.py:1`

## 行为变化

### 变更前

账户列表只显示账户、SSO 和 CPA 文件，模型探测结果只能从任务日志判断。

### 变更后

账户列表直接展示模型 Tag，并能在页面上刷新模型、测试单个模型和更新全部模型的当前额度状态。

## 兼容性与数据影响

- CPA 凭证原文件结构不变。
- 模型状态写入 CPA 目录下的 `.model_capabilities/` 隐藏目录。
- CPA ZIP 导出只包含 `xai-*.json`，不会包含模型缓存。
- 没有 CPA 的账户保持原行为，并显示“需先获取 CPA”。

## 验证结果

- Python 语法检查通过。
- JavaScript 语法检查通过。
- 后端模拟测试覆盖模型列表、模型测试、额度状态和禁止直连。
- 本地 Chrome 页面验证通过。
- 线上真实账户返回模型 `grok-4.5`。
- 单模型测试返回 HTTP 200。
- 额度状态刷新结果为 1/1 个模型可用。

## 回滚方式

- 恢复变更前的 Web 后端、前端和 CPA 探测文件。
- 删除 `cpa_xai/model_capabilities.py`。
- `.model_capabilities/` 只包含派生缓存，必要时可以安全删除并重新获取。

